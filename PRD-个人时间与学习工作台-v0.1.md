# 个人时间与学习工作台 PRD

版本：v0.5 讨论稿｜日期：2026-09-21（沿用初稿文件路径）

本文区分已确认需求、建议默认值和待确认范围。建议默认值不代表用户已确认。当前阶段仅确定需求，不开始开发。

## 1. 目标与范围

为一位使用者提供电脑端个人工作台，以周日历管理课程、兼职、作业和求职。核心差异是网页自身具备AI对话和图片任务提取能力：用户拖入图片，网页调用配置的模型解析事项、日期和任务，通过对话补齐信息，再填入任务或日历。SQL和英语仅预留入口，显示“敬请期待”，不实现教学功能。

必须从空白数据开始，不预填真实安排或示例成果。不得依靠开发阶段Codex人工读取图片、写入结果来冒充网页具备该能力。网页的AI流程须独立于当前Codex对话运行。

第一轮开发的目标是打通已确认的主要使用流程，而非用页面数量衡量“完成80%”。上线前必须验收：创建项目与子任务→从清单拖入日历→阻止冲突→完成或待确认→退回清单并手动重排→重启后恢复数据→撤销操作。

已排除：手机适配、多端同步、云端账号、团队协作、支付、自动把未完成任务顺延至明天。完整语音教学、全天候自主调度不在建议首版范围内。

例外：用户主动使用AI功能时，需要向所配置的模型服务发送本次图片、文本和必要的日程上下文；“本地版”不意味着AI调用离线。网页内告知实际服务提供方与发送范围，不默认发送全部历史记录。

## 2. 已确认的产品规则

| 编号 | 规则 |
|---|---|
| R01 | 打开网页直接进入连续七天日历，可从任意日期开始，例如周三至下周二；不是固定自然周。支持向过去和未来跨周浏览及跨周拖动任务。 |
| R02 | 默认显示08:00—24:00；纵轴只标整点，拖动和时长调整以30分钟吸附，不显示半小时文字刻度或显著半小时分隔线。 |
| R03 | 大项目可包含多个小任务，小任务独立拖动排期、打勾完成。 |
| R04 | 到达安排结束时间而未勾选完成，标记“待确认”，不自动判断失败。 |
| R05 | 用户确认未完成后，剩余事项回到待安排清单，显示未完成提醒；不自动另排时间。 |
| R06 | 用户从清单将未完成事项拖到自己选择的时间重新安排。 |
| R07 | 可以记录部分进度，例如练习10题已做5题；剩余部分重新排期，原时段记录保留。 |
| R08 | 与已有安排冲突的位置不能放置。无效落点显示灰色和叉号；有效落点显示蓝色预览。无效放置返回原位置，不弹冲突确认窗。 |
| R09 | 改变时间块长度同样不得产生冲突。 |
| R10 | 支持撤销，包括拖动、时长修改、完成和删除等数据操作。 |
| R11 | 第一版提供API Key配置，供网页内AI对话及图片识别创建任务使用；SQL和英语教学均暂缓。 |
| R12 | 固定课程必须每周自动重复。清单中未完成事项优先，其后按截止日期排序。 |
| R13 | 初始数据为空；桌面快捷方式启动。不做弹窗或声音；支持用户明确要求的暂缓跟进提醒，展示渠道及关闭网页时的行为见第10节，不再一概排除主动提醒。 |
| R14 | 所有AI新增、修改或删除建议先生成可编辑草稿，用户确认采用后才更改正式数据；对话或识别完成不构成采用。 |
| R15 | 重复课程必须支持学期开始、结束日期；用户可通过图片与对话提供这些信息。 |
| R16 | 首版AI分析用户图片、文本与本地日程，不做主动联网搜索；不按图片业务类别限制上传或识别。 |
| R17 | 时间信息模糊时AI先适当追问；用户暂时无法回答时，允许保存已知内容，标记待补充，并设置稍后跟进，不无限追问或编造日期。 |
| R18 | 支持语音转文字输入：用户口述，转写为可编辑文本，由用户发送；不是实时语音通话或语音教学。 |
| R19 | 固定事件（课程／班次）到期成为历史事件，不要求逐次打勾；待办任务才适用待确认、部分完成及退回清单流程。 |
| R20 | 半小时规则只约束拖动吸附；编辑和导入可保留09:15等精确时间，不自动取整。 |

## 3. 页面与界面（建议方案）

### 3.1 周日历主界面

- 顶部：当前七天日期范围、向前／向后浏览、回到今天、日期选择、新建、撤销、设置入口；不强制以周一为起点。
- 左侧：待安排清单和项目；可展开子任务，显示未完成、截止日期、预计时长。
- 中央：七天日历，08:00—24:00，只显示整点刻度，半小时为隐形吸附位置；支持拖入、移动、拉伸时长。
- 右侧：可收起的AI对话面板，支持文字、语音转文字、拖入或选择图片、查看任务草稿、回答澄清问题；到期跟进可在此继续原任务讨论。任务详情另用侧栏内切换视图，避免层叠弹窗。
- 待确认事项有独立入口或计数，避免旧周的待确认任务被遗忘。
- 空列表提供新建引导；保存失败、AI失败提供明确状态和重试入口。

### 3.2 项目／任务详情

建议项目字段：名称、类别、说明、子任务列表、完成数量。

建议小任务字段：名称、所属项目、预计时长、截止时间（可选）、备注、完成状态、进度（可选）、历史安排。

安排与截止日期分开：移动工作时间不能改变截止日期。截止日期本身不占用日历时段。

待定：是否需要独立任务、同一任务同时安排多个未来时间块、项目多层嵌套。建议允许独立任务，只保留项目→小任务两层，首版不做无限嵌套。

### 3.3 学习入口

保留SQL、英语两个入口或区域，显示“敬请期待／后续增加”，不提供假练习、假反馈、假学习进度。SQL运行、题库、英语纠错、语音教学和学习统计均不属于当前首版；通用语音转文字输入属于首版。

### 3.4 网页内AI助手（核心）

已确认：网页内对话、拖入图片、自动解析任务名称和相关时间、通过对话澄清后填入，不要求用户回Codex完成每一步。

已确认流程：图片预览→解析中→结构化可编辑草稿→仅对缺失或矛盾字段追问→用户手动修改或通过对话修改→用户明确采用→写入任务或安排→提供撤销。即使信息完整，也不可跳过采用步骤；不使用弹窗确认。

草稿需提供名称、任务／固定事件类型、日期、开始结束时间、截止日期、重复规则、学期起止日期、所属项目、备注等适用字段的编辑入口。每条可取消勾选；建议支持采用所选条目。来源图片可放大并与草稿对照。AI推测值与原图明确值应可区分，未给出的信息留空或追问。

- 区分截止日期与实际工作时段；只有截止日期的作业不能擅自占用截止时刻。
- 模糊年份、相对日期、时区、缺失时长保留不确定性，不按电脑今天日期随意推断截图语境。
- 一张图片可提取多个事项和重复课程；保留来源图及提取依据，便于核对。
- 首版支持多轮补充、查看现有日程及提出安排草稿；修改既有安排同样先展示可编辑变更，用户采用后执行。
- 模型建议同样必须通过确定性冲突检查。拖动以半小时吸附；原始截止时间保留精确分钟。非半点固定事件的录入方式见第9节建议，不得静默取整。
- 重复提交或重复图片不得静默创建重复任务；提供候选重复项供处理。
- 图片内文字只作为资料，不作为改变程序规则、发送密钥或执行其他操作的指令。
- 无密钥、模型不支持图片、网络失败、输出无效时，在面板内说明并允许重试或手动编辑，不假装识别成功。
- 首版只分析用户提供资料和本地日程；主动联网搜索、打开外部链接或访问外部账户不纳入首版。
- 图片类别不限；课表、作业通知、面试通知、兼职排班仅为验收样本。无法提取日程的图片应正常解释或询问用途，不强行生成任务。不需要为每种图片单独创建skill。

### 3.5 设置与数据

建议配置项：AI服务地址、模型名、API Key、测试连接；日历时间显示；数据导出和恢复。图片解析要求所选模型支持图片输入，测试应分别检查文字与图片能力；不能假设所有兼容接口均支持图片。

API Key遮挡显示，可替换和删除；不写进前端源代码、日志或普通备份。由本地服务保管，具体存储方案开发前确定。连接失败不得影响日历功能。

建议自动保存到本地持久存储；备份包含任务、安排、进度和历史，不包含密钥。恢复前提示影响，并生成恢复前备份。禁止仅依赖容易清理的浏览器临时数据作为唯一记录。

## 4. 核心流程与状态

任务完成状态与安排状态分开记录，避免“时间到了”被误认为“任务完成”。

### 4.1 新建与安排

创建项目→添加小任务→输入预计时长→进入待安排清单→拖到有效时间→保存安排。

### 4.2 到期确认

待办任务的安排结束且未完成→待确认；固定事件到期只成为历史事件。

- 用户点完成：任务完成，历史时间块保留完成标记。
- 用户确认未完成：历史时间块保留未完成记录；剩余事项进入待安排清单，不继续占用未来时间。
- 用户记录部分完成：保存累计进度，剩余事项进入清单；避免重复生成同一任务。
- 用户未处理：持续保留待确认，可从统一入口找到，不自动挪动。

网页关闭期间到期的安排，在下次打开时读取当前日期和时间，与安排结束时间比较，补标待确认。页面打开或重新获得焦点时也重新核对。没有学习计时器、秒表或后台累计时长。

建议使用电脑系统时间及明确时区；系统可自行通过网络校时。独立网络时间查询不是到期判断的必要条件，若用户要求应用自行联网校时，再增加来源及离线回退设计。

历史时间块仅保留曾安排的记录，首版不做实际投入时长统计，不要求用户填写工时。

### 4.3 拖动与冲突

按整个候选时间区间判断冲突，而不只检查鼠标落点；相邻时段边界接触允许放置。建议所有有效安排都互斥。

有效候选显示蓝色轮廓与起止时间；无效候选显示灰色、叉号和简短原因。检查必须先于保存，不能先覆盖已有任务再回滚。

### 4.4 撤销

建议撤销最近20次数据操作，首版仅保证当前运行会话内有效。一次拖动只计一步；部分完成及退回清单作为一个完整操作撤销。AI已发出的请求与调用费用无法撤销，采用后的文本修改可撤销。

## 5. 本地数据结构（概念）

- 项目：标识、名称、类别、说明。
- 任务：标识、项目、标题、截止日期、预计时长、目标数量、完成数量、完成状态。
- 安排：标识、任务、起止时间、状态、确认结果。
- 学习记录：主题、资料、笔记、练习内容、反馈（具体按范围确定）。
- 设置：日历偏好、模型配置；凭证单独管理。

日历和清单显示同一套任务数据，不能拖动一次就复制出新的任务。写入失败时显示未保存，并保留可恢复内容。

## 6. 剩余范围选择

核心AI范围已确认：可编辑草稿、采用后写入、不限图片类别、不联网搜索、重复课程有学期结束日期。

固定事件与任务区别、精确时间例外、草稿自动保存、图片核对以及纵向滚动已确认。新增语音转文字、延后跟进和连续七天跨周视图，具体设计见第10节。时区建议默认电脑时区，界面可查看和修改；跨日／08:00之前事件应保留准确数据并在范围外区域可访问，不能静默丢弃。

直接连接当前Codex会话暂不作为核心依赖；网页调用用户选择的模型服务完成自己的AI流程，日程和对话状态本地保存。是否提供Codex读写入口后续另定。

## 7. 首版验收标准

- 首次打开进入空白连续七天日历，默认08:00—24:00；仅整点刻度，30分钟吸附；可显示周三至下周二并跨周移动任务。
- 固定课程在后续周自动出现；清单未完成事项优先，其次截止日期。
- 子任务可从清单排入空闲时段，移动和缩放后刷新仍保留。
- 与任一现有时间区间重叠时显示灰色叉号且无法落下；不改变原安排。
- 待办到期只转待确认；包括应用关闭期间已到期的任务；固定课程到期不加入待确认清单。
- 确认未完成后清单有提醒，未来日历没有任何自动生成的顺延安排。
- 部分进度保存正确，重排不清零，不复制任务；历史记录保留。
- 完成、删除、移动等可以撤销，相关清单、进度和日历同步恢复。
- AI配置测试有成功／失败反馈；密钥不明文展示；AI不可用时日程继续可用。
- 从空白状态上传开发时未预填结果的图片，由网页真实调用模型，提取事项并经澄清或采用写入；全过程不依赖Codex人工代操作。
- 图片只有截止日期时不擅自创建工作时段；信息缺失、识别失败和冲突可在对话面板处理。
- AI生成草稿以及编辑草稿时正式日历不变；采用后内容必须与用户最终编辑版本一致。
- 批量草稿可逐条核对与选择，课程按确认的学期起止范围重复，不无限生成。
- 图片类别未列入样本也可识别；不含任务的图片不得凭空生成日程。
- 关闭Codex对话后，网页AI仍能在本地服务运行且模型连接有效时独立使用。
- SQL和英语仅显示待开放入口；不弹提醒窗，不计时，不展示虚构学习成果。
- 应用重启恢复已有数据；备份可在测试数据上完整恢复。
- 未确认的SQL执行、直接助手连接等功能，不得以静态演示冒充已实现。
- 语音转写结果可编辑、可取消，用户发送前不触发任务采用或日程写入；拒绝麦克风权限时保留文字输入。
- 暂缓任务保存缺失信息、来源、上下文摘要及明确跟进时间；到期后网页内出现跟进卡片，可继续安排、再次延后或取消跟进。
- 跟进不依赖模型记忆、API在线或旧聊天仍在上下文中；重启后补查到期记录，不重复刷出相同提醒。

## 8. 开发前完成条件

用户确认关键范围→更新为v1.0确认稿→核对界面草图与交互→验证图片模型调用、结构化提取和任务写入闭环→开始完整实现。图片能力验证需用户提供配置或自行填写有效密钥，不在聊天中索取明文密钥。

80%目标按已确认验收项衡量。剩余部分应主要是样式打磨和非核心便利功能，不能把持久保存、主要操作或已承诺的AI连接留到最后。

## 9. 双视角流程推演（设计评审，尚非实际产品测试）

### 9.1 使用者视角：建议补充的行为

1. 已确认固定事件和待办任务分开：课程／班次无需每次打勾，到期仅成为历史事件；作业／练习才进入待确认和未完成清单。
2. 草稿对照：支持图片放大、逐项编辑、取消无关事项、一键采用所选；不要求用户逐条回复大量对话。
3. 只知道模糊截止日期：先适当追问具体日期；仍无法确认则保留原文和待补充标记，采用后进入清单，可设置跟进。无需为了保存强填工作时间或时长，拖入日历时才需要时长，建议默认30分钟且可修改。
4. 自然语言更改：例如“这门课周三停一次”，生成仅该次取消的草稿，不改整学期；用户采用后执行。
5. 界面空间：七天列保持完整，左右侧栏可收起；08—24点允许纵向滚动，不为挤进一屏而令半小时时间块无法阅读。
6. 失败与恢复：没有配置模型时仍可手工管理；草稿、对话和编辑内容自动保存，关掉网页后可以继续；调用失败保留图片和文字。
7. 找回内容：清单支持名称搜索、项目筛选和已完成记录入口；历史日历可查，删除可撤销。
8. 已确认时间精度：30分钟只约束拖动吸附，手工／AI草稿保留09:15等固定事件时间，绝不擅自改成09:00或09:30。

### 9.2 开发者视角：可靠性要求

1. 模型负责提取与提出变更，程序负责字段校验、冲突判断、重复规则和保存；模型不能直接写数据库。
2. 草稿和正式数据分开，采用读取用户当前编辑后的草稿。后台模型返回旧答案不得覆盖手工修改；草稿有版本状态。
3. 采用前重新检查冲突和被修改对象的版本。用户对话期间改了日历时，不得用旧草稿覆盖新状态，需在面板内更新并重新采用。
4. 一次采用作为一个完整事务保存，重复点击或网络重试不得重复新增；支持整批撤销。无效条目明确列出，不静默漏存。
5. 固定课程以重复规则和单次例外保存；设置学期结束日期，支持仅本次／本次及以后，不对过去记录批量改写。
6. 截止日期可精确到分钟；只有日期时保留日期精度，不编造23:59。相关时区与原始文字可追溯。
7. 时间状态通过读取当前时钟重新计算；页面持续打开时也轻量刷新状态，无须学习计时器、后台累计或网络时间依赖。
8. 模型配置需验证实际图片能力；接口兼容不等于模型支持视觉。首版支持明确的接口协议，其他协议需适配，不能承诺任意Key均可直接使用。
9. 真实验收覆盖：完整图片、模糊信息、多任务、无关图片、重复导入、发生冲突、手工改草稿、学期结束、接口失败以及重启恢复。

### 9.3 更新后的首要验收场景

从空白进入→用户自行配置模型→拖入一张新课表→网页提取→用户手改一项并补充学期起止日期→采用→正确生成重复课程→再导入作业通知→只生成有截止日期的待办→用户拖入空闲时段→到期标待确认→退回清单并手动重排。过程中不需要Codex替网页识图或写数据。

## 10. 新增需求细化：跟进、语音和连续日期视图

### 10.1 暂缓及主动跟进

场景：图片写“周五交作业”→AI追问哪一个周五／必要细节→用户说尚未想好，过几天再问→保存待补充任务及跟进计划。

- “过几天”不是可执行的时间。AI应提出明确日期和时间供用户确认，例如“两天后的某时”，也可由用户直接指定，不静默猜定。
- 按已确认的采用规则，任务与跟进时间以可编辑草稿展示，用户采用后才持久保存。需补充任务即使暂不设跟进也可保存在清单。
- 跟进记录与任务工作时段、任务截止日期分开；提醒继续安排不等于自动安排执行。
- 保存任务标识、仍缺失的字段、用户原话／上下文摘要、来源引用、跟进时间、状态；不能仅在聊天里回复“记住了”。
- 到期显示站内跟进卡片：“你之前暂缓的某任务还未安排，要继续吗？”支持继续讨论、重新指定跟进时间、取消跟进；完成／删除任务时同步关闭相应跟进。
- 到期检测与卡片呈现由程序完成，即使模型接口暂不可用也能显示；需要对话时再调用模型并加载该任务资料。
- 到期未处理保留在待跟进区，不每次刷新反复创建新消息；再次延后必须有新时间。
- 已确认不弹窗、不发声音：网页运行时按时显示站内卡片；网页关闭后不提醒，下次打开补显示到期未处理事项。不为提醒增加后台常驻、系统通知或系统调度；电脑关机或休眠时不要求准点显示。

### 10.2 语音转文字

- AI输入区提供麦克风按钮；首次使用请求浏览器麦克风权限。
- 开始录音→明确的录音中状态→停止→转写中→可编辑文字→用户发送。可取消，取消不发送消息、不创建任务。
- 首版重点支持中文口述及夹杂SQL、课程名称等词；转写错误允许手改，不把误识别文本当作已采用操作。
- 转写失败、权限拒绝、麦克风不可用时保留可操作状态并提供重试和键盘输入。
- 语音识别与图片／对话能力分别验证；当前聊天模型API不一定支持音频，可能需要单独的转写服务或本地方案。具体提供方、成本和实现方式在开发前技术验证中确定。
- 若采用远程转写，明确音频发送到哪个服务；默认转写后不长期保留录音，文字按聊天草稿保存。
- 不包含语音播报、全双工通话、英语口音评分。录音状态指示不等于学习用时统计。

### 10.3 七天滑动窗口与跨周滚动

- 固定展示连续七个日期列，起点可任意选择，例如周三、周四、周五、周六、周日、周一、周二。星期随真实日期计算。
- 已确认支持时间轴纵向滚动，并支持向上一周／下一周浏览和拖动，不只用“上一自然周／下一自然周”整页跳转。
- 建议交互：普通纵向滚动先浏览08—24点；抵达上下边界后继续有意滚动，日期窗口按一天前移或后移，回弹防抖避免一次滚轮跳多天。提供按天导航和日期选择作为精确替代。此手势为实现建议，需要交互原型验证，不能与拖动时的小时滚动混淆。
- 拖动中靠近日期视图左右边缘可逐日平移七天窗口，靠近上下边缘只滚动小时；释放前明确显示目标日期及起止时间。未有效释放不写入。
- 按天平移保留全部七列；重复事件、冲突检查和撤销依据真实日期，不以屏幕列号存储。
- 跨月、跨年、学期结束日均按真实日期处理；返回今天建议恢复今天起连续七天，记忆上次浏览位置作为后续偏好。

### 10.4 已确认的提醒运行边界

用户已确认网页关闭后无需提醒。首版只提供站内跟进卡片：页面运行时检查当前时间；重新打开时补查并显示到期未处理记录。不需要系统通知权限，也不为提醒保持后台进程。该决定不取消已保存的跟进记录。
